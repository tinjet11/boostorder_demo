name: boostorder_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  envied: ^1.1.1
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5

dev_dependencies:
  build_runner: ^2.4.13
  envied_generator: ^1.1.1
  hive_generator: ^2.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
